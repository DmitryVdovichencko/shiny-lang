import React, { FC } from 'react';
import { Typography } from '@material-ui/core';

const AccessRequired: FC = (props) => <Typography variant="h3">You don't have access!</Typography>

export default AccessRequired;