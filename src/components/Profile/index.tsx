import React, { FC } from 'react';
import { Typography } from '@material-ui/core';

const Profile: FC = (props) => <Typography variant="h3">welcome to shiny lang user Dimas!</Typography>

export default Profile;